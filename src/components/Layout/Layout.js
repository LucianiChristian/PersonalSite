import './Layout.scss';
import Sidebar from '../Sidebar/Sidebar.js';

export default function Layout() {
    return (
        <div className="container">
            <Sidebar />
        </div>
    );
}